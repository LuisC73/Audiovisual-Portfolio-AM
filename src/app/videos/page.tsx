'use client';

import { Container, Navbar } from '@/components';

const VideosPage = () => {
  return (
    <Container className="flex flex-col">
      <Navbar page="videos" />
    </Container>
  );
};

export default VideosPage;
